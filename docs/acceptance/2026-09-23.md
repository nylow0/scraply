# PR #27 workflow acceptance - 2026-09-23

Application implementation: `cd8381b5b53b9d29281e3b459471255e007e2030`. This record covers the updated research and idea workflows in PR #27. It is not acceptance of Scraply 1.0 for publication. Two uniquely named QA projects used the existing browser-development profile and its encrypted provider connections. Existing projects, account state, and credentials were left intact.

## Real browser and provider journeys

| Journey | Explicit limit | Actual use and result |
| --- | --- | --- |
| Babysit discovery, review, generation | GPT-6 Luna/medium for research, then GPT-6 Sol/medium for ideas; 36 model calls, 10 Exa searches, 20 minutes; target 1 | 12 calls and 8 searches through generation. Two problems found, one chosen after evidence review, one distinct idea saved (1/1). The selected problem stayed **insufficient-evidence** because the qualifying buyer observation came from one shop. |
| Babysit Explain | GPT-6 Sol/medium; no search | One completed call, turn `65180e37-bc28-4f67-bd2d-e2861c69cb2b`. It cited the saved source and separated the observed one-shop burden from unproven prevalence and demand. |
| Babysit saved-evidence reevaluation | GPT-6 Luna/medium; 1 call, 0 searches, 10 minutes | One completed call. Comparing and applying the result retained the original finding and created evidence snapshot `a8fbe76c-a14f-4fdf-97ac-770e9c625ed6`. It separated advice about approval before work from the reported paperwork after work. |
| Babysit Rethink with newer research | GPT-6 Sol/medium; no search | The first submission failed locally before provider dispatch with SQLite `no such column: created_at`: zero provider calls and no saved turn or version. A regression reproduced the cause; source ordering was corrected. One approved retry completed in one call: turn `613a7b73-5eee-4fdc-ae02-b2657a675e94` and v2 idea `8fc5543e-20fa-4e78-8f96-56b480c57ebd` both cite the applied snapshot and source `74970a75-4e21-4cae-91f7-278134974156`. Original v1 `41ead887-bc85-4aac-9f83-e37539de6ae8` still opens with its original mechanism. |
| Vibe known-problem run | GPT-6 Sol/medium for ideas and review; 8 calls, 0 searches, 20 minutes; target 1 | Two calls, no searches. Known-problem and idea tasks advanced automatically to a reviewed terminal result: 1/1 distinct idea, validated 1, duplicates 0, missing 0. The premise remained labeled **user-asserted**, not discovered demand. |
| Vibe explicit new-evidence follow-up | GPT-6 Sol/medium; 5 calls, 2 Exa searches, 10 minutes | A proposed 3-call limit was rejected by preflight before dispatch. One run at the minimum admitted 5-call limit used 5 calls and 2 searches. Both angles completed with four source results each, but no result met the independent shop-owner evidence requirement. Zero replacement findings were saved. No more calls were made. |

These journeys used **22 model calls and 10 searches** in total. The running UI initially showed reservations; the actual counts above come from settled allowance and saved usage. The provider reported tokens and attempt counts but no monetary price, so no dollar cost is claimed. Native subscription calls have no guaranteed token or billing ceiling within these work limits.

## Persistence, exports, and UI

After reload, Babysit's applied research request, original and revised idea mechanisms, both completed conversation turns, source references, and version history remained accessible. Its research-only follow-up no longer reset the visible saved-idea count or described a no-search reevaluation as a zero-source search. The original idea was unchanged. Ideas JSON and Markdown retained the v1/v2 lineage, turns, and snapshots. A real export initially showed the new citation as unavailable; a same-project source-reference fix was verified by re-export: JSON resolved cited source `74970a75-4e21-4cae-91f7-278134974156` to its title and URL, and Markdown linked it.

Vibe's original 1/1 result and idea survived the separate research session. Before/after JSON exports had the same original workflow outcome and preexisting idea fields; the later export added only the new source-reference projection and a separate research-follow-up outcome. The first **Keep current research** action merely closed the comparison and left review pending. A persisted decision fix was then exercised on that saved no-result request without a provider call. After reload, the request said **Kept current research**, its comparison reopened read-only, and the terminal state said **Partial result. Current research was kept. No new evidence snapshot was applied.** Research JSON confirmed the original Vibe session still `finished/target-met`, the follow-up `finished/partial`, `reviewDecision=kept-current`, zero result findings, and no change in active snapshot or snapshot count. Real Vibe Apply remains unverified because this bounded search produced no qualifying finding; Babysit Apply was verified in the browser.

At 1280 x 720 CSS pixels, the status and first idea were visible together, with a short list summary and detailed content inside the idea. At 390 x 844, the navigation defaulted closed, document/body scroll width was 390, and the idea remained readable without horizontal document overflow. The drawer toggle, backdrop and Escape behavior preserved an unsent Explore draft. Two browser tabs held different QA projects simultaneously; each idea detail and conversation still opened after the other tab switched projects. Local captures are in ignored `build/qa/final-ideas-desktop.png` and `build/qa/final-ideas-narrow.png`.

## Checks and installed build

- Final `bun run check` passed ESLint, both TypeScript projects, Svelte diagnostics (0 errors, 0 warnings), 248 unit, 227 integration, and 105 renderer tests: **580 passed, 0 failed**. The opt-in Exa live probe was skipped; Exa was exercised in the bounded browser journeys.
- `git diff --check` and `git diff --cached --check` passed. The staged Gitleaks 8.30.1 scan passed. A separate history scan passed over accessible local refs and reflogs in this nonshallow checkout, without fetching other refs; it is not a complete audit of every ref that could later become public.
- From the clean application commit above, `bun run build:installed` passed native tests, production build, package integrity, local installation, and installed-file verification. The package manifest records Scraply **0.3.0**, runtime **0.2.0**, source `cd8381b5b53b9d29281e3b459471255e007e2030`, `dirty=false`, and four hashed artifacts. The installed executable is `%LOCALAPPDATA%\Programs\Scraply\Scraply.exe` (SHA-256 `f6e8aa8efa2c94a6c7b7fcc0a34c489bf127d8918fc0975b41c0ac6b8814ac3f`). Installer and portable artifacts are under `release/`.
- The checkout's development host was restarted after installation at `http://127.0.0.1:5174/`; its health session was `c181ae45-59ad-47cf-9893-b8c587ac310d`. The saved Babysit project reopened after that restart. The unrelated service at 5173 was untouched.

One earlier development host exited after the successful saved Rethink. Its stderr was lost when the launcher replaced `build/browser-dev/server.log` on restart; the app log had no later fatal request. The saved versions reopened after recovery. The cause of this one host interruption remains unknown. This acceptance did not exercise native desktop-only dialogs or permission flows, measure broad idea quality or customer demand, or establish provider dollar cost. The publication and clean-master release gates in [RELEASE.md](../../RELEASE.md) remain separate. No version, tag, merge, or release changed.
